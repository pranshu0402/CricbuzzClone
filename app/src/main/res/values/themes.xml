<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CricbuzzClone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CricbuzzClone" parent="Base.Theme.CricbuzzClone" />

    <style name="regular_semibold_small">
        <item name="fontFamily">@font/averta_semibold</item>
        <item name="android:textSize">11sp</item>
        <item name="android:letterSpacing">0.02</item>
    </style>

    <style name="original_regular_small">
        <item name="fontFamily">@font/averta_regular</item>
        <item name="android:textSize">9sp</item>
        <item name="android:letterSpacing">0.02</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerSize10Percent" parent="">
        <item name="cornerSizeTopLeft">10%</item>
        <item name="cornerSizeTopRight">10%</item>
        <item name="cornerSizeBottomLeft">10%</item>
        <item name="cornerSizeBottomRight">10%</item>
    </style>

    <declare-styleable name="CricConstraintLayout">
        <attr name="customBackgroundColor" format="color" />
        <attr name="customBackgroundRadius" format="dimension" />
    </declare-styleable>

</resources>